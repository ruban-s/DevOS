# {{PROJECT_NAME}} — Conventions

> Lives at `DEVOS/PROFILE/CONVENTIONS.md` in the project repo. Discovered during the Spec interview, corrected by the owner, binding on all runs. Facts only — no aspirations.

- **Stack:** {{LANGUAGES_FRAMEWORKS — e.g. TypeScript on bun, Python on uv}}
- **Runners:** {{HOW_TO_RUN — test/lint/typecheck/build commands, exact}}
- **Test runner for ISA `test` rows:** {{REPO_NATIVE_RUNNER — the command Test Strategy `test` rows invoke}}
- **Style:** {{FORMATTER_LINTER_CONFIG — what enforces style, not opinions}}
- **Boundaries:** {{ASK_BEFORE_LIST — e.g. prod deploys, public pushes, .env, migrations}}
- **Glossary:** {{TERMS_THAT_ALREADY_BIT — entry on confusion only, else omit}}
