{
  "$comment": "DevOS hook wiring (v0.1). Claude-Code-shaped; other harnesses read hook stdout. ${CLAUDE_PROJECT_DIR} resolves to the target repo root — hooks + tools run repo-local from DEVOS/. Wire manually (Phase 7 automates this): copy this file's entries into the harness settings. Every hook fails open and exits 0 — a hook must never be why a turn breaks. Env kills: ISAGATE_OFF=1, VERIFICATIONGATE_OFF=1, ALGORITHMNUDGE_OFF=1.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [{ "type": "command", "command": "bun ${CLAUDE_PROJECT_DIR}/DEVOS/hooks/AlgorithmNudge.hook.ts" }]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": "bun ${CLAUDE_PROJECT_DIR}/DEVOS/hooks/ISASync.hook.ts" },
          { "type": "command", "command": "bun ${CLAUDE_PROJECT_DIR}/DEVOS/hooks/CheckpointPerISC.hook.ts", "timeout": 30 }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [{ "type": "command", "command": "bun ${CLAUDE_PROJECT_DIR}/DEVOS/hooks/AlgorithmNudge.hook.ts" }]
      }
    ],
    "Stop": [
      {
        "hooks": [{ "type": "command", "command": "bun ${CLAUDE_PROJECT_DIR}/DEVOS/hooks/StopGates.hook.ts" }]
      }
    ]
  }
}
